# Контекст
- Ведомость объемов работ (QuantityTakeoff) версия 3.01: требуется визуализация ресурсов, расположенных на одном уровне с Work (`Section/Works/Resource`), чтобы отображались в таблице ВОР как самостоятельные строки (например, машины/оборудование, не привязанные к конкретной работе).
- Схема уже поддерживает `Works` с choice (`Work` | `Resource` | `FreeString`); XSL сейчас рисует только `Work` и вложенные `Resources/Resource`.
- Правки ограничены XSL; XSD не меняем; baseline XSD сохранён в schema/QuantityTakeoff.20251210134247.xsd.

# История
- 2025-12-10 13:42 — Снят baseline schema/QuantityTakeoff.20251210134247.xsd; предчек: check_garbage (OK), compare_xsd_types (без diff), check_unused_types_xmlschema (TGuid), xmlschema валидация (OK).
