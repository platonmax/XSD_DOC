# Контекст
- ЛС РИМ 3.02: требуется добавить тип “очищенной” строки (аналог TClearString из MarketAnalysis) для запрета спецсимволов и применять его к строковым элементам; уточнить состав символов по референсу (MarketAnalysis.xsd: TClearString pattern = [\p{L}\p{N}\p{P}\p{S}\s]*).
- Меняем только XSD/XSL РИМ, без правок документации/описаний (если не будет отдельного запроса).

# История
- 2025-12-10 15:30 — Создана папка задачи и зафиксирован стартовый контекст. Предчек/базелайн не снят (ожидаем подтверждения паттерна и охвата элементов).
